# Task 5 - variation 2: icmp-pinger

It's c/c++ implementation of icmp-pinger for Windows icmp API

To build and run, use `cmake`, and do remember to specify `CMAKE_CXX_COMPILER` and `CMAKE_C_COMPILER`

